import React from 'react'

export const CategoryCard = ({
    id,
    categoria,
    codigo,
    listas
}) => {
    return (
        <div>
            {categoria}
            {codigo}
        </div>
    )
}
